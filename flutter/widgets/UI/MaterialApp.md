# MaterialApp

```dart
MaterialApp(
  theme: ThemeData(...),
  home: HomeScreen(...),
  routes: {
    '/this': (context) => ThisScreen(...),
    '/that': (context) => ThatScreen(...),
    '/another': (context) => AnotherScreen(...),
  },
);
```
