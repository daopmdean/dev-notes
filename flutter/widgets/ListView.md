# ListView

```dart
ListView(
  scrollDirection: Axis.vertical,
  children: [
    /*...*/
 ],
);
```

```dart
ListView.builder(
  padding: EdgeInsets.only(
    /*...*/
  ),
  itemCount: /*...*/,
  itemBuilder: (context, index) {
    return SomeThing(
      /*...*/
    );
  },
);
```
